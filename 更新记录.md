# 更新记录

## v0.1.1 - 2026-08-03

首个可发布版本。完成 Vite + React + Astryx 债务记账 Web 应用的全部开发性变更。

### 新增

- 项目脚手架：Vite 6 + React 19 + TypeScript + React Router
- UI 框架：集成 [Astryx](https://github.com/facebook/astryx) (Meta 开源设计系统) 与 neutral theme
- 后端：Node.js + TypeScript 实现的 REST API（开发用 HTTP server，生产用 Vercel Function）
- 数据库：Postgres (生产) / PGlite (本地嵌入式) + Drizzle ORM
- 数据库 schema：`users` (含 role/admin、active 字段)、`debts` (方向/金额/已还/状态/日期/备注)
- 认证：HMAC 签名 session cookie + bcrypt 密码哈希；首次启动自动 seed 管理员账号
- 页面：登录页、统计总览、债务明细（CRUD + 搜索/筛选）、用户管理（仅管理员）
- 统计：借出/借入总额、未还金额、净额、按方向/状态/对方汇总、最近记录、Top 10 未还
- 部署配置：vercel.json + Vercel Function catch-all，配置 Neon Postgres 集成即可上线

### 技术决策

- 数据库本地用 PGlite（npm 嵌入式 Postgres），无需 Docker
- 前端表单混合使用 Astryx 组件（Button/Text/TextInput）与原生 input 配合自写 CSS
- 债务方向仅区分「借出（别人欠我）」与「借入（我欠别人）」，与用户确认一致
- 登录采用自建方案（HMAC + bcrypt），不引入第三方服务

### 验证

- TypeScript 编译：通过（`npx tsc -b`）
- Vite 生产构建：通过（`npm run build`，gzip 后约 157 KB）
- 端到端浏览器测试：通过（登录 → 总览统计 → 债务列表 → 退出登录）
- API 端到端：通过（登录、CRUD、统计接口均返回正确数据）
- Vercel 部署：已绑定 GitHub 仓库 `nothingtosayyy/jizhangben`，首次生产部署成功

### 已知待办

- Vercel Hobby 计划对 Serverless Function 数量有 12 个上限限制，本次用 catch-all function + 静态前端规避。后续如需拆分多 endpoint，需注意此限制。
- 默认管理员密码 `admin123456` 仅用于首次登录，登录后请立即通过"用户管理"页修改。
